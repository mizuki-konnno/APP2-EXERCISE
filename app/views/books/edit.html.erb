<p id="notice">
      <%= notice %>
</p>



<div class="container">
<div class="row">
  <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  <h2>Editing Book</h2>

  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
  <% end %>

  <%= form_with model:@book, local:true do |f| %>
  <table>
    <tbody>
      <tr><td><h5>Title</h5></td></tr>
      <tr><td><%= f.text_field :title %></td></tr>
      <tr><td><h5>Body</h5></td></tr>
      <tr><td><%= f.text_area :body %></td></tr>
      <tr><td><%= f.submit 'Update Book',class: 'btn btn-success' %></td></tr>
      <tr><td><%= link_to "Show",book_path(@book) %>|<%= link_to 'Back',books_path %></tr>
    </tbody>
  </table>

<% end %>
</div>
</div>
</div>








