<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<div class="container px-5 px-sm-0">
<div class="row">
<div class="col-md-3">
<h2>User info</h2>
<table class="table" style="white-space: nowrap">
<tr>
<td>
<%= attachment_image_tag current_user, :profile_image, fallback: "no_image.jpg", :size=>'60x60' %>
</td><td></td>
</tr>

<tr><td><h6>name</h6></td><td><h6><%= current_user.name %></h6></td></tr>
<tr><td><h6>introduction</h6></td><td><h6><%= current_user.introduction %></h6></td></tr>

</table>


    <p><%= link_to edit_user_path(current_user),class: "btn btn-outline-secondary btn-block" do %>
    <i class="fas fa-user-cog"></i><%end%></p>


<%= render 'books/new.html',book:@book %>

</div>
<div class="col-md-8 offset-md-1">
<h1>Books</h1>
<table class="table table-hover table-inverse small">

<thead>
<tr>
<th></th><th>Title</th><th>Opinion</th>
</tr>
</thead>
<% @books.each do |book| %>


<tbody>
<tr>
<td>
  <%= link_to user_path(book.user.id) do %>
    <%= attachment_image_tag book.user, :profile_image, fallback: "no_image.jpg", :size =>'30x30' %>
  <%end%>
</td>
<td><span><%= link_to book.title, book_path(book.id) %></span></td>

<td><span><%= book.body %></span></td>
<td></td>
</tr>
</tbody>
<% end %>
</table>

</div>
</div>
</div>